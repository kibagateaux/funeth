// contract RSAWithFees is FunFundingTest {
//     function setUp() public {
//         vm.prank(factory.owner());
//         factory.setFunLoanFee(300); // 3% fee on all credit extended
//         // checks if all math in tests is kosher based on real values or if adding fees changes anything
//         super.setUp();
//     }

// }
